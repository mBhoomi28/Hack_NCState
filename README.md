SIREN is a browser extension that scans the webpage to detect deception. It uses Gemini API
and SightEngine API to check for deepfake and factually incorrect information. 

nmp install
move to backend file
run node server.js
unpack load from chrome browser extension using "extension" file
